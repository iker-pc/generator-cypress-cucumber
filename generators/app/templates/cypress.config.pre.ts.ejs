import { defineConfig } from "cypress";

import commonProperties from "./cypress.config.common";

export default defineConfig({
    ...commonProperties,
    env: {
        name: 'pre',
        ...commonProperties.env,
    },
});