import { CustomPrompQuestion } from "../../types/CustomPrompQuestion";

const baseUrlQuestion: CustomPrompQuestion = {
    type: "input",
    name: "baseUrl",
    message: "What's is the base URL of your project?",
}

export { baseUrlQuestion };